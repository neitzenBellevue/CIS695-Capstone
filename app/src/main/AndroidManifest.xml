<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CIS695Capstone"
        tools:targetApi="31">
        <activity
            android:name=".settingsActivity"
            android:exported="false"
            android:label="Settings/Goals" />
        <activity
            android:name=".weightHistoryActivity"
            android:exported="false"
            android:label="Weight History" />
        <activity
            android:name=".weightEntryActivity"
            android:exported="false"
            android:label="Add Weight Entry" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Summary Screen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>